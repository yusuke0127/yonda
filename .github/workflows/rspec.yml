name: RSpec

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run RSpec
        shell: bash
        run: |
          docker-compose run web bundle exec rspec
        env:
          RAILS_ENV: test
